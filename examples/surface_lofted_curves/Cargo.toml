[package]
name = "surface_lofted_curves"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
splines = { path = "../../splines" }
render = { path = "../../render" }
primitives = { path = "../../primitives" }
cgmath = { version = "0.18.0" }
viewer = { path = "../../viewer" }
