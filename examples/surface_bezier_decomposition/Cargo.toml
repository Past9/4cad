[package]
name = "surface_bezier_decomposition"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
splines = { path = "../../splines" }
tessellate = { path = "../../tessellate" }
render = { path = "../../render" }
primitives = { path = "../../primitives" }
cgmath = { version = "0.18.0" }
viewer = { path = "../../viewer" }
